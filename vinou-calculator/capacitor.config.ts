import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vinou.calculator',
  appName: 'vinou-calculator',
  webDir: 'dist/vinou-calculator',
  bundledWebRuntime: false
};

export default config;
